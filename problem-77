#include <stdio.h>

int main() {
    int n, a[100], d, temp;
    scanf("%d", &n);

    for (int i = 0; i < n; i++) scanf("%d", &a[i]);

    scanf("%d", &d);

    for (int r = 0; r < d; r++) {
        temp = a[0];
        for (int i = 0; i < n - 1; i++) {
            a[i] = a[i + 1];
        }
        a[n - 1] = temp;
    }

    for (int i = 0; i < n; i++) printf("%d ", a[i]);

    return 0;
}

/*
Input:
5
1 2 3 4 5
2

Output:
3 4 5 1 2
*/
