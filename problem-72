#include <stdio.h>

int main() {
    int n, a[100], b[100], j = 0;
    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }

    for (int i = 0; i < n; i++) {
        if (i == 0 || a[i] != a[i - 1]) {
            b[j++] = a[i];
        }
    }

    for (int i = 0; i < j; i++) {
        printf("%d ", b[i]);
    }

    return 0;
}

/*
Input:
7
1 1 2 2 2 3 4

Output:
1 2 3 4
*/
