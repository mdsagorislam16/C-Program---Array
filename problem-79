#include <stdio.h>

int main() {
    int r, c, a[10][10], b[10][10], flag = 1;
    scanf("%d %d", &r, &c);

    for (int i = 0; i < r; i++)
        for (int j = 0; j < c; j++)
            scanf("%d", &a[i][j]);

    for (int i = 0; i < r; i++)
        for (int j = 0; j < c; j++)
            scanf("%d", &b[i][j]);

    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            if (a[i][j] != b[i][j]) {
                flag = 0;
                break;
            }
        }
    }

    if (flag == 1) printf("Equal");
    else printf("Not Equal");

    return 0;
}

/*
Input:
2 2
1 2
3 4
1 2
3 4

Output:
Equal
*/
