#include <stdio.h>

int main() {
    int n, a[100], x;
    scanf("%d", &n);

    for (int i = 0; i < n; i++) scanf("%d", &a[i]);

    scanf("%d", &x);

    for (int i = 0; i < n; i++) {
        if (a[i] != x) {
            printf("%d ", a[i]);
        }
    }

    return 0;
}

/*
Input:
6
2 3 2 4 2 5
2

Output:
3 4 5
*/
