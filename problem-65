#include <stdio.h>

int main() {
    int n, i;
    float sum = 0;
    int a[100];

    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        sum += a[i];
    }

    printf("Average = %.2f\n", sum / n);

    return 0;
}

/*
Input:
5
2 4 6 8 10

Output:
Average = 6.00
*/
