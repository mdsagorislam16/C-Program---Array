#include <stdio.h>

int main() {
    int n, a[100], b[100];
    scanf("%d", &n);

    for (int i = 0; i < n; i++) scanf("%d", &a[i]);

    for (int i = 0; i < n; i++) b[i] = a[i];

    for (int i = 0; i < n; i++) printf("%d ", b[i]);

    return 0;
}

/*
Input:
5
10 20 30 40 50

Output:
10 20 30 40 50
*/
